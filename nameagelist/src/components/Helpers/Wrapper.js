const Wrapper = props =>{
    return props.choldren;
};
export default Wrapper;
// the helper for wrapping in JSX ==> instead of react.fragment ==>clear our code   and <></> 
// empaty tages !
// react.portals ==> meaking ypu code for overlay components in our entire page  
